$(document).ready(function(){
    //initiate the plugin and pass the id of the div containing gallery images

    // THIS CODE ON index.js
  // $("#img_01").elevateZoom({ lensSize: 350, zoomType: "lens", containLensZoom: true, cursor: 'pointer' });
  // $("#img_02").elevateZoom({ lensSize: 350, zoomType: "lens", containLensZoom: true, cursor: 'pointer' });
});
